[package]
name = "sandboxed"
version = "0.1.0"
edition = "2021"
description = "A falling-sand cellular automaton compiled to WebAssembly"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
